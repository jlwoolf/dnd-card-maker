services:
  frontend:
    build:
      context: .
      dockerfile: ./frontend/Dockerfile
    container_name: dnd-card-maker
    ports:
      - "8080:80"
    restart: always
    # Environment variables can be passed here if your build script supports them
    environment:
      - NODE_ENV=production